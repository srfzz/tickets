spring:
  application:
    name: tickets
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
  profiles:
    active: dev
  mvc:
    problemdetails:
      enabled: true

server:
  port: 8080